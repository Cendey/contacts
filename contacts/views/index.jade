extends layout

block content
    h1= title
    p#welcome.top Welcome to #{title}
    form(id="enter" action=service.trim() + action.trim())
        div#top
        input(type="submit" id="nav" value="Enter")
